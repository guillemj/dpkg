# While correct, this is the simplest way to convey that the script is using
# bash syntax for shellcheck and editor highlighting, and to avoid duplicating
# the entries in automake and then having to rename the files.
bash-completion-with-hashbang /bin/bash [usr/share/bash-completion/completions/*]
